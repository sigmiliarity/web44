import { n as scanEnums, t as resolveOptions } from "./options-o0NnMBk6.mjs";
import { withMagicString } from "rolldown-string";
import { createUnplugin } from "unplugin";
import ReplacePlugin from "unplugin-replace";
//#region src/index.ts
/**
* This entry file is for main InlineEnum.
* @module
*/
/**
* The main unplugin instance.
*/
const InlineEnum = createUnplugin((rawOptions = {}, meta) => {
	const options = resolveOptions(rawOptions);
	const { declarations, defines } = scanEnums(options);
	const replacePlugin = Object.assign(ReplacePlugin.raw({
		include: options.include,
		exclude: options.exclude,
		values: defines,
		...options.replaceOptions
	}, meta), { name: "unplugin-inline-enum:replace" });
	return [{
		name: "unplugin-inline-enum",
		enforce: options.enforce,
		transform: {
			filter: { id: {
				include: options.include,
				exclude: options.exclude
			} },
			handler: withMagicString((s, id) => {
				if (!(id in declarations)) return;
				for (const declaration of declarations[id]) {
					const { range: [start, end], id, members } = declaration;
					s.update(start, end, `export const ${id} = {${members.flatMap(({ name, value }) => {
						const forwardMapping = `${JSON.stringify(name)}: ${JSON.stringify(value)}`;
						const reverseMapping = `${JSON.stringify(value.toString())}: ${JSON.stringify(name)}`;
						return typeof value === "string" ? [forwardMapping] : [forwardMapping, reverseMapping];
					}).join(",\n")}}`);
				}
			})
		}
	}, replacePlugin];
});
//#endregion
export { InlineEnum, resolveOptions };
