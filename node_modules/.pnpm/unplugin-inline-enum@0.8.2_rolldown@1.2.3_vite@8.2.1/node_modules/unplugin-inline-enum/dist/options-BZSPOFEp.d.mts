import { FilterPattern } from "unplugin";
import { Options } from "unplugin-replace";

//#region src/core/options.d.ts
/**
* Represents the options for the plugin.
*/
interface Options$1 {
  include?: FilterPattern;
  exclude?: FilterPattern;
  enforce?: "pre" | "post" | undefined;
  /**
  * The mode used to scan for enum files.
  * @default 'fs'
  */
  scanMode?: "git" | "fs";
  /**
  * The directory to scan for enum files.
  * @default process.cwd()
  */
  scanDir?: string;
  /**
  * The pattern used to match enum files.
  * @default ['**\/*.{cts,mts,ts,tsx}', '!**\/node_modules']
  */
  scanPattern?: string | string[];
  replaceOptions?: Options;
}
type Overwrite<T, U> = Pick<T, Exclude<keyof T, keyof U>> & U;
/**
* Represents the resolved options for the plugin.
*/
type OptionsResolved = Overwrite<Required<Options$1>, Pick<Options$1, "enforce">>;
/**
* Resolves the options for the plugin.
* @param options - The options to resolve.
* @returns The resolved options.
*/
declare function resolveOptions(options: Options$1): OptionsResolved;
//#endregion
export { OptionsResolved as n, resolveOptions as r, Options$1 as t };